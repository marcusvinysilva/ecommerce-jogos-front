import Title from "./styles";

export const GlobalTitle = ({children}) => {

    return (
        <div>
            <div>
            <Title> {children}</Title>
            </div>
        </div>

    )



}